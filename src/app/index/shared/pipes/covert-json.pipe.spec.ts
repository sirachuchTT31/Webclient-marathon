import { CovertJsonPipe } from './covert-json.pipe';

describe('CovertJsonPipe', () => {
  it('create an instance', () => {
    const pipe = new CovertJsonPipe();
    expect(pipe).toBeTruthy();
  });
});
